/// Service Locator trung tâm để inject dependencies (Repositories, UseCases).
class ServiceLocator {
  ServiceLocator._();

  static void setup() {
    // Sẽ thêm khởi tạo dependencies khi làm feature Home & PinEntry
  }
}
